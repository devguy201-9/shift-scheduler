[workspace]
resolver = "3"
members = [
    "shared",
    "data-service",
    "scheduling-service"
]

[workspace.dependencies]
sqlx = { version = "0.7.3", features = [
    "postgres",
    "macros",
    "uuid",
    "chrono",
    "runtime-tokio",
    "offline"
] }
